# In-Action Response Registry (Audit Repair)

Purpose: evidence-based registry of bounded in-workflow responses and state-scoped clarifications.

## A) Bounded canonical response groups

Design rule:
- New confirmation-like or route-like response groups must be implemented through `bot/services/decision_resolver.py`.
- This registry records canonical outputs, not per-flow synonym lists.
- Handlers must consume canonical outputs only and must not parse raw `ano` / `nie` / `ok` / `schvalit` / `zrusit` / multilingual variants locally.
- If an existing decision family fits, reuse it. If none fits, add a bounded family to the DecisionResolver before adding handler branches.
- Shared decision callbacks must remove their inline markup after a handled decision and when a callback is stale, expired, or no longer owns an active FSM state.

| Response group | Category | Status | Entry mode | Canonical tokens / values | Source evidence | Notes |
|---|---|---|---|---|---|---|
| `invoice_preview_confirmation` | in-action response group | implemented | mixed (text + voice) | `schvalit`, `upravit`, `zrusit`, `unknown` | `process_invoice_preview_confirmation()` resolves with context `invoice_preview_confirmation`; voice routes to this handler from `waiting_confirm`. | Preview is now draft-review decision. Backward-compatible aliases: `ano` -> `schvalit`, `nie` -> `zrusit`. `upravit` enters draft edit backend without DB/PDF side effects. |
| `invoice_postpdf_decision` | in-action response group | implemented | mixed (text + voice) | `schvalit`, `upravit`, `zrusit`, `unknown` | `process_invoice_postpdf_decision()` resolves with context `invoice_postpdf_decision`; voice routes from `waiting_pdf_decision`. | `upravit` enters bounded edit subflow; full `edit_invoice` operation map is still partial in runtime. |
| `invoice_customer_alias_confirm` | in-action response group | implemented | mixed (text + voice) | `yes`, `no`, `unknown` | `process_invoice_customer_alias_confirm()` resolves with context `invoice_customer_alias_confirm`; voice routes from `waiting_customer_alias_confirm`. | Legacy/explicit alias confirmation path for one close supplier-scoped customer candidate. `yes` stores the cleaned extracted customer candidate as a supplier-scoped confirmed alias for that contact; it must not store the full STT transcript. High-confidence fuzzy or bounded LLM contact resolution may now skip this separate prompt and store the cleaned candidate only after invoice preview approval. `no` returns to customer clarification without saving. |
| `mark_existing_invoice_paid_confirm` | in-action response group | implemented | mixed (text + voice + decision buttons) | `yes`, `no`, `unknown` | `invoice_mark_existing_invoice_paid_confirm()` resolves with DecisionResolver context `mark_existing_invoice_paid_confirm`; voice routes from `waiting_mark_existing_invoice_paid_confirm`; decision buttons dispatch canonical `decision:yes` / `decision:no` to the same handler. | `yes` writes bot-local `invoice_followup_state.payment_status=paid` through `InvoiceFollowupService.mark_paid()`. When owner OAuth Drive archive is enabled/configured, it may enqueue the existing local PDF for archive-worker upload; otherwise it records the local Drive archive stub. `no` returns to `/menu`. `unknown` repeats the two-button choice. No bank confirmation, bank matching, invoice content edit, delete, or local PDF deletion is performed. |
| `invoice_due_date_followup_decision` | in-action route decision | partial | Telegram callback buttons only | `mark_paid`, `remind_later`, `mute` | The automatic invoice follow-up scheduler sends overdue-invoice reminder cards with callback data scoped to one invoice id; `invoice_followup_callback()` validates the callback user owns the invoice before persisting follow-up state. | Phase 1 intentionally does not add text/voice parsing for these three choices because one user can receive multiple overdue cards. Buttons carry the invoice id and avoid ambiguous text replies. Callback payloads now carry a timestamp and legacy/expired payloads fail closed before `mark_paid`, `remind_later`, or `mute` side effects. `mark_paid` records paid state and, when owner OAuth Drive archive is enabled/configured, may enqueue the existing local invoice PDF for archive-worker upload; otherwise it records the local Drive stub status. `remind_later` snoozes; `mute` stops reminders. No bank matching or local PDF deletion occurs. |
| `business_profile_selection` | in-action bounded value selection | partial / target-schema implemented | text/reply keyboard | one Python-provided accessible workspace id/label, `add_profile`, `cancel`, or `unknown` | `BusinessProfileStates.waiting_selection`; `business_profile_selection()` reloads membership-scoped candidates before mutation. | Exact accessible selection switches immediately. Unknown/inaccessible values remain in selector. Active foreign FSM states block entry without clearing their state/data. |
| `business_profile_switch_confirm` | in-action response group | partial / target-schema implemented | text + voice | `yes`, `no`, `unknown` | Voice switch stores one pending workspace id and resolves through shared `yes_no` DecisionResolver before `WorkspaceContextService.set_active_workspace(...)`. | `yes` revalidates membership and switches; `no` preserves the previous active profile; `unknown` remains. Text/reply-keyboard exact selection does not require the extra voice confirmation. |
| `additional_workspace_activation_confirm` | onboarding completion response group | partial / target-schema implemented | text + voice | `yes`, `no`, `unknown` | Additional supplier onboarding creates an isolated workspace/profile transaction first, stores its workspace id in FSM, and uses shared `yes_no` before optionally making it active. | `no` keeps the previous active workspace. Existing persisted installations require migration apply before this flow is deployed. || `supplier_profile_edit_field` | in-action field selection | implemented | mixed (text + voice) | one supplier-profile field token or `unknown` | `supplier_profile_edit_field()` first uses shared Python field aliases/fast-path, then bounded resolver context `supplier_profile_edit_field`; voice routes from `SupplierProfileEditStates.field` to the same handler. | This selects which field to edit only. The new field value remains text-first in `SupplierProfileEditStates.value`. |
| `supplier_profile_edit_confirm` | in-action response group | implemented | mixed (text + voice) | `yes`, `no`, `unknown` | `supplier_profile_edit_confirm()` resolves with context `supplier_profile_edit_confirm` before saving one targeted supplier-profile field; voice routes from `SupplierProfileEditStates.confirm` to the same handler. | Python validates the field value before confirmation and updates only the selected supplier-profile field after `yes`. Exact field-value entry remains text-first. |
| `contact_intake_confirm` | in-action response group | implemented | mixed (text + voice + decision buttons) | `yes`, `no`, `unknown` | `process_contact_intake_confirm()` resolves through shared `yes_no` with context `contact_intake_confirm`; callbacks and voice converge on the same helper. | Document/semantic contact drafts are written only after `yes`. |
| `contact_confirm` | in-action response group | implemented | mixed (text + voice + decision buttons) | `yes`, `no`, `unknown` | `contact_confirm()` resolves through shared `yes_no` with context `contact_confirm`; callbacks and voice converge on the same helper. | Manual contact wizard final confirmation. |
| `contact_registry_confirm` | in-action response group | implemented | text + decision buttons; voice may confirm only at this final state | `yes`, `no`, `unknown` | `contact_registry_final_confirm()` resolves through shared `yes_no` with context `contact_registry_confirm`; decision callbacks dispatch canonical tokens to the same helper. | Registry candidate/detail/DIČ/email/IBAN/contact-person states are text/button-only. The dedicated transaction re-checks workspace IČO/name conflicts before insert/update. |
| `edit_invoice:invoice_level` | in-action response group | partial (2 implemented, 1 planned) | mixed entry with bounded clarification | `edit_invoice_number`, `edit_invoice_date`, `edit_invoice_contact`, `unknown` | Product + contract docs map these as invoice-level subflow ops under `edit_invoice`; runtime currently implements `edit_invoice_number` + `edit_invoice_date` (strict Phase 1 `DD.MM.RRRR`). | `edit_invoice` remains top-level reserved token; runtime must execute via bounded subflow only. Integrity-sensitive fields fail safe on ambiguity/conflict. |
| `edit_invoice:item_level` | in-action response group | implemented | mixed entry; precision-sensitive steps are text-first | `replace_service`, `edit_item_description`, `edit_item_quantity`, `edit_item_unit_price`, `edit_item_total_amount`, `unknown` | Product + contract docs define full item-level map; runtime implements service/description and numeric item edit operations with bounded value capture. | Item targeting required for precision-sensitive item edits. Single-item can default to first item; multi-item requires explicit selection or bounded clarification. |
| `idle_attachment_accounting_proposal` | in-action response group | partial | text after idle attachment classification | `yes`, `no`, `unknown` | Shared idle attachment router uses `decision_resolver.resolve_yes_no(...)` before entering accounting document preview processing. | Used only after LMM classified an idle attachment as `receipt` or `incoming_invoice`. The proposal may show explicit `Áno` / `Nie` reply buttons. `yes` starts candidate extraction/preview; `no` cleans temp staging. |
| `accounting_document_category_preview_decision` | in-action response group | partial | mixed (text + voice) inside accounting document preview | `save_with_category`, `change_document_category`, `change_line_item_category`, `save_without_category`, `cancel`, `unknown` | Accounting Document Intake preview resolves through `resolve_accounting_document_category_preview_decision(...)`; voice routes active FSM transcripts back into the same helper. | This replaces the old receipt preview approve/edit/cancel behavior. Category changes update only the preview state; confirmed metadata is written only after final save. Reply-keyboard cancel text may be consumed by the earlier shared global-cancel router; that path must remove the keyboard, clear the FSM, and clean accounting temp staging for every accounting-intake state. |
| `accounting_document_category_unknown_decision` | in-action response group | partial | mixed (text + voice) after `unknown_review` category candidate | `choose_existing_category`, `create_new_category`, `save_as_unknown_review`, `cancel`, `unknown` | Accounting Document Intake uses `resolve_accounting_document_category_unknown_decision(...)` before showing the normal save preview when the LMM could not choose a bounded category. | Existing categories are offered first. New workspace categories are confirmation-gated and never created by the model. |
| `accounting_document_category_create_confirm` | in-action response group | partial | text + voice confirmation after typed new category label | `yes`, `no`, `unknown` | New category creation uses shared `resolve_yes_no(...)` with context `accounting_document_category_create_confirm`. | The category label itself is text-first; voice in the label state asks for typed text. A `yes` creates only a workspace-scoped category and applies it to the current preview. |
| `accounting_document_category_selection` | in-action value selection | partial | mixed (text + voice) inside category selection states | one Python-provided category id, `create_new_category`, `back`, `cancel`, or `unknown` | `resolve_accounting_document_category_selection(...)` receives `allowed_categories` from Python and matches category id/display label values from that list plus bounded navigation controls. | This is a bounded selection, not a top-level action. System and active workspace categories only are valid; new category creation still moves to the typed-label plus confirmation flow before persistence. |
| `accounting_document_category_similar_decision` | in-action response group | partial | text + voice after duplicate/similar category label warning | `use_existing_category`, `create_new_anyway`, `back`, `unknown` | Accounting Document Intake uses `resolve_accounting_document_category_similar_decision(...)` when a typed new label resembles an existing category. | Similar-category handling prevents accidental duplicates while allowing explicit user override. |

Accounting category UX note: current runtime may present Telegram reply keyboards for the accounting category preview, unknown-category decision, loud duplicate save-anyway choice and new-category yes/no confirmations, similar-category warning, and reasonable category selection lists that end with create-new/back controls. These buttons send visible text back into the active FSM state; handlers still consume only canonical DecisionResolver outputs or Python-provided category ids, and terminal save/cancel/failure outcomes remove the custom keyboard.
| `attachment_route_choice` | in-action response group | partial | text after idle contract/contact-source classification | `create_contact`, `save_contract`, `cancel`, `unknown` | Shared idle attachment router resolves contact/contract route choice through the DecisionResolver family. | Slice 1 runtime supports `create_contact` and `cancel`. `save_contract` is reserved and fails explicitly without standalone contract save. |
| `attachment_document_type_choice` | in-action response group | partial | text after unknown idle attachment classification | `receipt`, `incoming_invoice`, `contract`, `contact_source`, `cancel`, `unknown` | Shared idle attachment router uses a bounded clarification family when the classifier returns `unknown`. | This clarification only selects a document type candidate. Python still maps it to a proposal and asks before save/create side effects. |
| `active_fsm_navigation_interrupt` | active-FSM navigation/recovery group | implemented | active text + active voice transcript | `cancel_current_flow`, `show_main_menu`, `resume_start_status`, `pass_through` | `resolve_active_fsm_navigation()` in `bot/services/decision_resolver.py`; `ActiveFsmMessageMiddleware` and `handle_active_fsm_text_update()` in `bot/services/active_fsm_guard.py`; `handle_voice()` calls the same guard after STT. | Python executes only existing behavior: `cancel_current_flow` reuses `cancel_current_state()`, `show_main_menu` reuses `cmd_menu()` / `MENU_MESSAGE`, `resume_start_status` reuses `cmd_start()` / staged status, and `pass_through` leaves the active state handler in control. Stale states use shared activity metadata before parsing. Fresh active-FSM safe switch confirmation is intentionally deferred until previous FSM state/data restore can be proven by tests. |
| `global_state_cancel` | state-control response group | implemented | command + text/voice | `cancel`, `unknown` | `/cancel` and shared DecisionResolver-backed cancel wording route through `bot/handlers/state_control.py`; active-state voice transcripts are checked before state-specific voice handlers except exact typed destructive confirmations. Tests cover the shared resolver matrix and state-control outcomes. | State-aware escape hatch. It clears ordinary FSM states, cleans temporary intake staging, delegates newly generated post-PDF invoice cancellation to existing cleanup, and exits persisted invoice edit mode without deleting the stored invoice. |

| `work_time_manual_range_preview` | in-action response group | partial | mixed (text + voice + decision buttons) | `approve`, `edit`, `cancel`, `unknown` | `WorkTimeStates.waiting_manual_range_confirm` resolves through `resolve_approve_edit_cancel(...)`; callbacks dispatch pre-canonicalized approve/edit/cancel tokens to the same handler path. | Manual work-time ranges and duration-only totals are exact-value records. Slot extraction is bounded LLM-first with explicit mode values for multilingual date/start/end/duration normalization, while Python validates, shows a full preview, and writes only after approval. Parser fallback is retained only for non-LLM/dev paths. Edit returns to typed/time input; unknown replies repeat the full pending preview with context and buttons; cancel clears state. |
| `work_time_close_preview` | in-action response group | partial | mixed (text + voice + decision buttons) | `approve`, `edit`, `cancel`, `unknown` | `WorkTimeStates.waiting_close_preview_confirm` resolves through `resolve_approve_edit_cancel(...)`; callbacks dispatch to the same state-aware close handler. | Closing by explicit end time or duration is confirmation-gated. Python calculates total minutes and writes only after approval. Unknown replies repeat the full pending preview with context and buttons. Explicit start/end rows store gross time and report net time after the configured lunch deduction; duration rows keep the confirmed net duration stable. |
| `work_time_lunch_break_initial_choice` | in-action setup decision | partial | mixed (text + voice + decision buttons) | `yes`, `no`, `unknown` | `WorkTimeStates.waiting_lunch_break_initial_choice` resolves through `resolve_yes_no(...)`; callbacks dispatch canonical `decision:yes` / `decision:no` to the same handler path. | First report setup asks once whether lunch break should be deducted. `yes` asks for minutes before report generation; `no` stores lunch disabled and continues the pending report. |
| `work_time_lunch_break_value` | in-action exact value entry | partial | text/voice bounded by Python parser | minutes 0-180 or `unknown` | `WorkTimeStates.waiting_lunch_break_value` uses Python minute parsing before saving settings and continuing the pending report. | Lunch-break minutes are a user report setting, not payroll/legal HR compliance. Invalid values do not save. |
| `work_time_lunch_break_update_confirm` | in-action response group | partial | mixed (text + voice + decision buttons) | `approve`, `edit`, `cancel`, `unknown` | `WorkTimeStates.waiting_lunch_break_update_confirm` resolves through `resolve_approve_edit_cancel(...)`; callbacks dispatch pre-canonicalized approve/edit/cancel tokens to the same handler path. | Setting, changing, or disabling the fixed lunch-break deduction is preview-confirmed before save. It affects future report net-hour calculation; duration-only rows retain their confirmed net duration. |
| `work_time_delete_month_confirm` | destructive in-action response group | partial | mixed (text + voice + decision buttons) | `yes`, `no`, `unknown` | `WorkTimeStates.waiting_delete_month_confirm` resolves through `resolve_yes_no(...)`; callbacks dispatch canonical `decision:yes` / `decision:no` to the same state-aware delete handler. | Monthly work-time deletion is destructive. Python previews month/year, row count, and total hours first; `yes` deletes only bound current-workspace DB work-time rows/events for that selected month, `no` clears state without side effects, and generated Excel reports are not treated as canonical data. |
| `work_time_open_day_conflict_choice` | in-action route decision | partial | mixed (text + voice + decision buttons) | `close_day`, `fill_time`, `skip_day`, `cancel`, `unknown` | `resolve_work_time_open_conflict_choice(...)` normalizes conflict choices when a user tries to open a new day while a previous day is still open. | The active work-time FSM consumes only canonical route choices. `close_day` asks for close input, `fill_time` asks for a manual range, `skip_day` marks the open row skipped, and `cancel` clears state. |
| `work_time_missing_days_choice` | in-action route decision | partial | mixed (text + voice + decision buttons) | `fill`, `skip`, `cancel`, `unknown` | `resolve_work_time_missing_days_choice(...)` provides a bounded family for future missing-day prompts and is covered in the central DecisionResolver matrix. | MVP records the family and keyboard path but does not implement broad automatic missing-day detection. It must not become payroll/legal compliance logic. |
## B) Deterministic (non-LLM) in-action confirmations

This section documents legacy/manual deterministic confirmations. It is not a template for new work.

No new confirmation-like response group should be added here unless explicitly approved as a deterministic non-semantic exception. New product/runtime flows must default to the Canonical DecisionResolver contract.

| Response group | Category | Status | Entry mode | Allowed values | Source evidence | Notes |
|---|---|---|---|---|---|---|
| Contact manual confirmation legacy note | migrated to shared response group | implemented | text + voice + decision buttons | `yes`, `no`, `unknown` | See `contact_confirm` in section A. | No handler-local yes/no parser remains. |
| `supplier_onboarding_confirm` | in-action response group | implemented | text | `yes`, `no`, `unknown` | `onboarding_confirm()` resolves with shared `yes_no` DecisionResolver context `onboarding_confirm`. | Bootstrap/setup flow confirmation. |
| `delete_user_database_exact_confirmation` | destructive exact confirmation | implemented | text only | exact phrase `vymazať databázu` | `DeleteUserDatabaseStates.waiting_exact_confirmation`; `confirm_delete_user_database()` calls `UserDataDeletionService` only after exact typed match. | Explicit exception: no yes/no resolver and no voice confirmation. Voice in this state is rejected before STT and top-level routing. |

## C) Slot clarification and bounded value groups

| Value/slot group | Category | Status | Entry mode | Source evidence | Notes |
|---|---|---|---|---|---|
| Invoice analytics yearly fast-path period selection (`current_year`, `previous_year`, `unknown`; explicit `YYYY` parsed by Python) | internal invoice analytics value group | implemented | mixed (text + voice after `invoice_analytics` top-level routing) | The invoice analytics handler may call bounded resolver context `invoice_summary_period_selection` for simple calendar-year count/total questions; Python converts the canonical period token into exact `issue_date` range and executes tenant-scoped aggregation. | This is internal action-parameter canonicalization, not a separate public top-level action and not a phrase whitelist. LLM may only choose from Python-provided period values; Python validates and executes. Unsupported periods such as month/quarter/custom ranges stay in `invoice_analytics` or return safe guidance. |
| Invoice analytics customer identity resolution | internal invoice analytics identity group | partial | mixed (text + voice/STT after `invoice_analytics` routing) | Bounded extractor returns minimal `customer_reference` or `null`; Python runs exact → normalized → confirmed alias → high-confidence fuzzy; `invoice_analytics_customer_resolution` is only the final bounded fallback over current-tenant contacts. | A resolved contact becomes a trusted `contact_id` prefilter. `null` means general analytics; an explicit unresolved reference asks for clarification and does not analyze all invoices. No alias/contact/invoice write occurs, and the planner may not re-filter `customer_name` or `contact_id`. |
| Invoice unresolved slot clarification (`service_term`, `customer_name`, `delivery_date`, `due_days`, `quantity`, `unit_price`, `quantity_unit_price_pair`) | in-action response group | implemented | mixed (text + voice) | Invoice FSM clarification handlers and prompts; quantity×price pair uses dedicated bounded resolver `resolve_quantity_unit_price_pair(...)`; voice routes for `waiting_service_clarification` and `waiting_slot_clarification`. | State-bounded only; not global free-form extraction. |
| `create_invoice` Phase 2 intake shape (`singleton` + optional bounded `items[]`) | in-action response/value contract group | partial (Phase 1 implemented) | mixed intake (text + voice via STT) | Runtime supports backward-compatible dual-shape intake: singleton item fields remain valid; optional `biznis_sk.items[]` (max 3) is accepted as candidate segmentation shape; parser and preview/save paths normalize to internal list shape with bounded clarification/fail-safe behavior. Voice routes both from idle top-level input and from `InvoiceStates.waiting_input` after `/invoice`. | Implemented Phase 1: bounded multi-item intake + persistence. Legacy single-item path remains compatible. |
| Contact intake exact/missing values (`name`, `ico`, `dic`, `ic_dph`, `address`, `email`, `iban`, `contact_person`) | in-action value capture group | implemented | text-first | Manual/document handlers and registry required/optional states validate one bounded field at a time; voice in exact-value registry and missing-field states is rejected before STT/top-level routing. | Python validates IČO/DIČ/IČ DPH/email/address and contact-specific MOD-97 IBAN, normalizes contact IBAN to uppercase without spaces, permits optional skip, exposes `/menu`, and applies the five-minute inactivity expiry. No registry or LLM inference creates DIČ, IČ DPH, email, IBAN, or contact person. |
| Generic exact-value text fallback | in-action safety fallback | implemented | voice fallback only | none | `handle_voice()` refuses unhandled active FSM states with a Slovak text-required prompt instead of falling through to top-level routing. | Prevents voice from clearing or overriding active typed-value states. Business-specific phrase routing remains outside `voice.py`. |

## D) Audit correction focus

The previously reported in-action set did not connect service-alias functionality because `/service` was a separate command flow and did not define semantic canonical in-action tokens.
This is expected and should be documented as a manual command flow, not as a missing in-action resolver group.

## E) Reserved/partial contract notes for `edit_invoice` map

- `edit_invoice` is a reserved top-level token; runtime behavior is bounded in-action/subflow edits.
- `edit_existing_invoice` is explicit top-level action for persisted invoice edit by number reference; DB lookup and supplier scoping are Python-only.
- Invoice-level operations are documented separately from item-level operations.
- Invoice-level mapped operations:
  - implemented: `edit_invoice_number`
  - implemented: `edit_invoice_date`
  - planned: `edit_invoice_contact`
- Item-level mapped operations:
  - implemented: `replace_service`, `edit_item_description`, `edit_item_quantity`, `edit_item_unit_price`, `edit_item_total_amount`
- `edit_item_description` mutation semantics remain explicit:
  - `set`
  - `replace`
  - `clear`
- Precision-sensitive item operations require item targeting.
- Single-item invoices may default to first item; multi-item invoices require explicit selection or bounded clarification.
- Destructive/integrity-sensitive edits must fail safe (halt current edit step + bounded clarification), never silent auto-fix.
- Minimal bounded output shape for this response family:
  - `target_item_index`
  - `operation`
  - `value`
- Numeric item operations are runtime-implemented with deterministic validation/recalculation in Python.

## F) Runtime note for `create_invoice` dual-shape intake

- Phase 1 runtime dual-shape support is implemented:
  - singleton remains valid,
  - optional bounded `items[]` (max 3) is supported,
  - Python remains final validator/workflow owner.
- Unclear item boundaries, quantity semantics, service resolution ambiguity, total mismatch, or render-safety overflow must trigger bounded clarification/fallback, not silent acceptance.
- For explicit persisted invoice delete commands (SK/EN/UK/RU), canonical top-level action is `delete_existing_invoice`; runtime delete remains Python-guarded with explicit confirmation.

## Contact registry search/tax clarification - 2026-07-18

`registry_candidates` now distinguishes exact identities from bounded suggestions. One exact identity may continue to detail; a single suggestion still requires its candidate button. Multiple exact names remain selectable. This navigation remains deterministic and is not a DecisionResolver family.

The detail preview may show source `slovak_rpo + financna_sprava` only when validated tax data was merged for the exact selected IČO. Disabled, unconfigured, unavailable, malformed, conflicting, or empty tax lookup retains `slovak_rpo`, keeps IČ DPH empty, and preserves the existing typed-DIČ continuation. Final save remains `contact_registry_confirm` through shared `yes_no`.

The tax note under the detail preview is field- and source-aware. A validated Financial Administration DIČ is described as obtained from an official source; a missing IČ DPH is described only as not found for the selected exact IČO and is never presented as inferred non-VAT status. Manual DIČ guidance appears only when DIČ is actually absent. The preview must not show a generic “missing required data” warning when all required registry fields, including DIČ, are already valid.



## Contact registry background proposal - 2026-07-29

`contact_registry_monitor_proposal` is a deterministic asynchronous in-action decision with bounded callback values `yes` and `no`. It reuses the canonical `yes_no` vocabulary but stores proposal context by opaque UUID rather than FSM state. Button callbacks only are supported in V1; free text and voice do not resolve this background proposal. Python revalidates actor, workspace, TTL, contact version, identity, and conflicts before any update.

## Active-FSM contextual help - 2026-08-02

The bounded active-FSM control family includes `describe_active_flow`, `describe_expected_input`, `contextual_info_help`, existing navigation outcomes and `pass_through`. Python maps the current state to an action label, step, expected input kind/description and controls. Informational help keeps the FSM unchanged and shows `Hlavné menu`; ordinary invoice references, values, dates, times, names, descriptions, amounts and confirmation decisions pass through to the state owner. Exact `/cancel`, `/menu`, `/start` and `/issue` retain deterministic priority.
